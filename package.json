{
  "name": "kingmakers",
  "private": true,
  "version": "0.1.0",
  "description": "킹메이커스: 민심 경매 — 정치 브로커 온라인 보드게임",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm build:libs && pnpm -r --parallel dev",
    "build:libs": "pnpm --filter @kingmakers/engine build && pnpm --filter @kingmakers/data build",
    "build": "pnpm -r build",
    "test": "pnpm build:libs && pnpm -r test",
    "typecheck": "pnpm --filter @kingmakers/engine typecheck && pnpm --filter @kingmakers/data typecheck && pnpm --filter @kingmakers/server typecheck && pnpm --filter @kingmakers/client typecheck",
    "ai:report": "tsx scripts/ai-selfplay-report.ts",
    "ai:report:json": "tsx scripts/ai-selfplay-report.ts --json"
  },
  "devDependencies": {
    "tsx": "^4.23.0"
  }
}
